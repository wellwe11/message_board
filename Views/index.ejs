<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>

    <%- include("./components/nav") %>

    <ul>
        <% messages.forEach((message, index) => { %>
            <%- include("./components/message", { message, index }) %>
        <% }); %>
    </ul>
  </body>
</html>
